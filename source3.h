#ifndef SOURCE3_H
#define SOURCE3_H

// Structure to hold environmental data (temperature in this case)
typedef struct {
    float temperature; // Store temperature in Celsius
} EnvData;

// Function declarations
EnvData get_environmental_data();  // Function to fetch environmental data

#endif 
#pragma once
